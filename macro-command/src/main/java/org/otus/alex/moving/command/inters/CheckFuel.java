package org.otus.alex.moving.command.inters;

import org.otus.alex.moving.command.CheckFuelAdapter;

public interface CheckFuel {

    Boolean isFuelExists( );

    void setIsFuelExists( boolean isFuelExists );

}
